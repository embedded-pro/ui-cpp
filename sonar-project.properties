sonar.projectKey=embedded-pro_ui-cpp
sonar.organization=embedded-pro
sonar.projectName=ui-cpp

sonar.sources=ui
sonar.tests=ui
sonar.test.inclusions=**/test/**
sonar.sourceEncoding=UTF-8

# Unlike the consumer repos, the UI is NOT blanket-excluded from coverage here: Tier 1 is tested.
# Only the untested backend glue is exempt.
sonar.coverage.exclusions=ui/backend/**,**/test/**
